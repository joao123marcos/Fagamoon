/*function raizQuadrada(valorNumerico){
    return valorNumerico ** 0.5
}

let variavel = raizQuadrada(4)
console.log(variavel)

let variavel2 = raizQuadrada(14)
console.log(variavel2)*/

//função declarada dentro de um objeto
/*var obj = {
    raizQuadrada: function(valorNumerico){
        return valorNumerico ** 0.5
    }
}
console.log(obj.raizQuadrada(14))*/

//xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
/*let lista = ['morango', 'maca', 'abacaxi', 'uva',
'pera', 'laranja', 'cafe','tomate']

function itera(parametro){
    //length é o tamanho do array
    for (let i = 0; i < lista.length; i++) {
        console.log(parametro[i]);
        
    }
}
itera(lista)*/

/* 1- forEach itera o array a cada elemento,
usando para isso uma variável insenda no parametro
da proŕia função forEach(variavel)*/

//lista.forEach(item => console.log(item))

