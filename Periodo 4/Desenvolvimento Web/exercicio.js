let vetor = ['morango', 'maca', 'abacaxi', 'uva', 'pera', 'laranja', 'cafe']

function itera(parametro)
{
    for(let i= 0; i < vetor.length; i++)
    {
        console.log(parametro[i])
    }
}

itera(vetor)